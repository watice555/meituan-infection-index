# 项目工作约定

- 原则上使用中文沟通。只处理用户要求的范围。
- 这是独立的美团指数项目，不得依赖同级日报项目的代码、虚拟环境或私有配置。
- 正式采集与历史恢复位于 `scripts/`，页面位于根目录；`meituan_infection_index_capture/` 是旧抓包工具，`meituan_infection_index_backup/` 是本地公开数据备份工具。
- Python 工具仅依赖标准库，使用 4 空格缩进。修改后按根 README 执行对应离线测试、语法检查和 `git diff --check`；网络调用使用 mock 或 fixture 验证。
- 没有用户要求，不得运行真实抓取、发送、手动部署或新增/触发定时任务；本地移动项目时可修正已存在任务的路径，保留原计划且不手动触发。
- 该仓库公开。不得提交 HAR、Proxyman 会话、私有请求模板、Token、Cookie、设备标识、账户信息、SQLite、CSV、生成数据、缓存、日志、临时报告、`.env` 或本地 LaunchAgent 定义。
- Pages 发布仅允许包含网页文件和生成的公开城市数据，不得恢复为上传整个工作目录。保留已有仓库、Pages、定时与权限配置，除非任务明确要求更改。
- 每个可独立验证的阶段完成后更新根目录 `IMPLEMENTATION_LOG.md`，包括阶段总结、实际验证、精确的 `Asia/Shanghai` 时间戳、实际模型名称（不能可靠确定时写“未知（运行环境未暴露）”），以及自动检测的主机名、操作系统和 CPU 架构。设备字段不得含用户名、序列号、MAC 或 IP 地址。
- 提交前运行 `git status --short`，只暂存本阶段源码、测试和文档，保留无关用户改动。记录任何跳过或受阻的验证后，直接创建有针对性的 Git 提交。
